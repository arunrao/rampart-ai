import { RestApiOverviewContent } from "@/components/docs/sections/rest-api-overview";

export const metadata = {
  title: "REST API overview | Rampart docs",
  description: "HTTP API overview, authentication, and example requests.",
};

export default function RestApiDocPage() {
  return <RestApiOverviewContent />;
}

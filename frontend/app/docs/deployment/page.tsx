import { DeploymentContent } from "@/components/docs/sections/deployment";

export const metadata = {
  title: "Deployment | Rampart docs",
  description: "Run Rampart with Docker Compose or cloud platforms.",
};

export default function DeploymentDocPage() {
  return <DeploymentContent />;
}
